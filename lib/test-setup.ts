// Per-test setup — kept for reference but not loaded by vitest.config.ts.
// Table truncation is handled directly in lib/database.test.ts to avoid
// cross-file database contamination when test files run in parallel.
